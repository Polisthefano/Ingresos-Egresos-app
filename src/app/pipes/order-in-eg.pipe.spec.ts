import { OrderInEgPipe } from './order-in-eg.pipe';

describe('OrderInEgPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderInEgPipe();
    expect(pipe).toBeTruthy();
  });
});
